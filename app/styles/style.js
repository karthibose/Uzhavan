import { StyleSheet } from 'react-native';
export default styles = StyleSheet.create({
  container: {
    backgroundColor: '#FFFFFF',
  },
  welcome: {
    fontSize: 20,
    textAlign: 'left',
    margin: 10,
  }
});